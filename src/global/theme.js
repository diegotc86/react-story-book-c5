import { colors } from "./colors";

export const theme = {
  light: {
    backGroundPrimary: colors.pink[500],
    primary: colors.gray[50],
  },

  dark: {
    backGroundPrimary: colors.gray[900],
    primary: colors.gray[50],
  },
};
